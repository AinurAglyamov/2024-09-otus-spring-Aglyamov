spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=LEGACY;
  sql:
    init:
      mode: always
      data-locations: classpath:test-data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
